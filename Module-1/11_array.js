// array
let tags = ['CSE', 'SWE', 'CIS', 'EEE', 'Civil', 'Architecture'];

console.log(tags[0]);

// array (for-of) loop
for (let tag of tags) {
  console.log(tag);
}

// array (for) loop
let meyeBondhu = ['Sajia', 'Shefa', 'Zafrin'];
for (let i = 0; i < meyeBondhu.length; i++) {
  console.log(meyeBondhu[i]);
}
